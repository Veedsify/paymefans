import { Link } from "react-router-dom";

const HomeRoutes = () => {
    return (
        <div>
            Enter
            <Link to="/login">Login</Link>
        </div>
    );
}

export default HomeRoutes;