import { Link } from "react-router-dom";

const Home_Layout = () => {
    return (
        <div>
            Enter    <Link to="/login">Login</Link>
        </div>
    );
}

export default Home_Layout;